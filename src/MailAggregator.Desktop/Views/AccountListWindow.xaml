<Window x:Class="MailAggregator.Desktop.Views.AccountListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Account Management" Height="480" Width="620"
        WindowStartupLocation="CenterOwner">

    <DockPanel Margin="16">
        <!-- Bottom: Buttons -->
        <Border DockPanel.Dock="Bottom" Margin="0,12,0,0" Padding="0,12,0,0"
                BorderBrush="{StaticResource SeparatorBrush}" BorderThickness="0,1,0,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="{Binding StatusText}"
                           Foreground="{StaticResource SubtleBrush}" FontSize="12"
                           VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Add Account" Style="{StaticResource PrimaryButton}"
                            Command="{Binding AddAccountCommand}" Margin="0,0,8,0" />
                    <Button Content="Edit" Style="{StaticResource ToolbarButton}"
                            Command="{Binding EditAccountCommand}" Margin="0,0,8,0" />
                    <Button Content="Delete" Style="{StaticResource DangerButton}"
                            Command="{Binding DeleteAccountCommand}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Account list -->
        <ListBox ItemsSource="{Binding Accounts}" SelectedItem="{Binding SelectedAccount}"
                 BorderThickness="1" BorderBrush="{StaticResource CardBorderBrush}"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="4,3" Padding="12,10" CornerRadius="6"
                            Background="{StaticResource CardBrush}"
                            BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1">
                        <DockPanel>
                            <!-- Status badge -->
                            <Border DockPanel.Dock="Right" CornerRadius="4" Padding="8,3"
                                    VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource SuccessBackgroundBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                                <Setter Property="Background" Value="{StaticResource ErrorBackgroundBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock FontSize="11" FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Enabled" />
                                            <Setter Property="Foreground" Value="{StaticResource SuccessTextBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                                    <Setter Property="Text" Value="Disabled" />
                                                    <Setter Property="Foreground" Value="{StaticResource ErrorBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>

                            <!-- Account info -->
                            <StackPanel>
                                <TextBlock Text="{Binding EmailAddress}" FontWeight="SemiBold" FontSize="14" />
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="{Binding ImapHost}" Foreground="{StaticResource SubtleBrush}" FontSize="12" />
                                    <TextBlock Text="  ·  " Foreground="{StaticResource SubtleBrush}" FontSize="12" />
                                    <TextBlock Text="{Binding AuthType}" Foreground="{StaticResource SubtleBrush}" FontSize="12" />
                                </StackPanel>
                            </StackPanel>
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</Window>
