<Window x:Class="MailAggregator.Desktop.Views.ComposeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="550" Width="650"
        WindowStartupLocation="CenterScreen">

    <DockPanel Margin="12">
        <!-- Top: Toolbar -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Send" Style="{StaticResource PrimaryButton}"
                    Command="{Binding SendCommand}" Margin="0,0,8,0" />
            <Button Content="Attach File" Style="{StaticResource ToolbarButton}"
                    Command="{Binding AddAttachmentCommand}" />
        </StackPanel>

        <!-- Bottom: Status -->
        <StackPanel DockPanel.Dock="Bottom" Margin="0,8,0,0">
            <TextBlock Text="{Binding StatusText}" Foreground="Gray" FontSize="12" />
            <ProgressBar IsIndeterminate="True" Height="4" Margin="0,4,0,0"
                         Visibility="{Binding IsSending, Converter={StaticResource BoolToVisibility}}" />
        </StackPanel>

        <!-- Form -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- From -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="From:" VerticalAlignment="Center" Margin="0,0,0,6" />
            <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding SenderAccounts}"
                      SelectedItem="{Binding SelectedSender}" DisplayMemberPath="EmailAddress"
                      Margin="0,0,0,6" />

            <!-- To -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="To:" VerticalAlignment="Center" Margin="0,0,0,6" />
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding To}" Padding="4,3" Margin="0,0,0,6" />

            <!-- Cc -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Cc:" VerticalAlignment="Center" Margin="0,0,0,6" />
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Cc}" Padding="4,3" Margin="0,0,0,6" />

            <!-- Bcc -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Bcc:" VerticalAlignment="Center" Margin="0,0,0,6" />
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Bcc}" Padding="4,3" Margin="0,0,0,6" />

            <!-- Subject -->
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Subject:" VerticalAlignment="Center" Margin="0,0,0,6" />
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Subject}" Padding="4,3"
                     FontWeight="SemiBold" Margin="0,0,0,6" />

            <!-- Attachments -->
            <ItemsControl Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                          ItemsSource="{Binding AttachmentPaths}" Margin="0,0,0,6">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="3"
                                Padding="6,2" Margin="0,0,4,4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding}" FontSize="11" VerticalAlignment="Center" />
                                <Button Content="X" FontSize="9" Margin="6,0,0,0"
                                        Command="{Binding DataContext.RemoveAttachmentCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource ToolbarButton}" Padding="4,1" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Body -->
            <TextBox Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                     Text="{Binding Body}" AcceptsReturn="True" AcceptsTab="True"
                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                     FontFamily="Segoe UI" FontSize="13" Padding="4" />
        </Grid>
    </DockPanel>
</Window>
